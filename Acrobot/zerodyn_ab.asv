clear;
close all;

params_pend;
tF = 10;
X0 = [pi/2;0];
tsPan = [0, tF];
S = odeset('RelTol',1e-8,'AbsTol',1e-8);  % low resolution
[tout,xout] = ode45(@(t,x) dx_abZD(t,x,p),tsPan,X0,S);

q2traj = xout(:,1);
q2dtraj = xout(:,2);

plot(tout,q2traj)
%plot(q2traj, q2dtraj)